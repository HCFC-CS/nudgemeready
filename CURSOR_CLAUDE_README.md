# Nudge me! handoff

This is the current Expo React Native code for the Nudge me! app.

## Open in Cursor

Open this folder in Cursor:

```powershell
C:\Users\HCCun\Documents\Codex\2026-05-04\create-appl-ios-from-the-following
```

## Install

```powershell
npm.cmd install
```

## Run

```powershell
.\node_modules\.bin\expo.cmd start --lan --clear --port 8082
```

If port `8082` is busy, use `8083`.

## Notes

- App name: Nudge me!
- Expo SDK: 54
- Main app code is in `src/`
- Shared UI components are in `src/components/`
- Screen files are in `src/screens/`
- Mock app data is in `src/data/mockData.ts`
