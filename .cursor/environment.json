{
  "name": "Nudge Me Ready",
  "install": "npm install && npx playwright install chromium",
  "terminals": [
    {
      "name": "expo-web",
      "command": "npx expo start --web --port 19006",
      "description": "Expo web dev server for the Nudge me Ready app (http://localhost:19006). Append ?screenshot=<ScreenId> to preview a specific screen."
    }
  ],
  "ports": [
    {
      "name": "expo-web",
      "port": 19006
    }
  ]
}
